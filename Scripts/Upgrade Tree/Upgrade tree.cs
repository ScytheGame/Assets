using UnityEngine;

public class UpgradeTree : MonoBehaviour
{

    public void Unlock(string ID, float Value = 0, bool OneTime = false)
    {
        if (ID.Equals ("Damage"))
        {
            
        }
    }
}
