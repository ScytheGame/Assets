using UnityEngine;
using System.Collections.Generic;

/*
public class SkillData : MonoBehaviour
{
    public string SkillName;
    public string SkillDescription;
    public Dictionary<string, float> attributes;
    public List<SkillData> prerequisites;

    public int requiredSkillLevel;
    public int requiredLevel;
    public bool isOneTime;
    public int CurrentLevel;

    public SkillData(string name, string description, int requiredSkillLevel = 0, int requiredLevel = 0, bool isOneTime = false, int CurrentLevel = 0)
    {
        SkillName = name;
        SkillDescription = description;
        attributes = new Dictionary<string, float>();
        prerequisites = new List<SkillData>();
        this.requiredSkillLevel = requiredSkillLevel;
        this.requiredLevel = requiredLevel;
        this.isOneTime = isOneTime;
        this.CurrentLevel = CurrentLevel;
    }

    public void AddAttribute(string key, float value)
    {
        attributes[key] = value;
    }
}
*/